// Keil 调试器初始化脚本
// 用于解决需要多次运行才能到达 main 的问题
// 
// 使用方法：
// 1. 在 Keil 中：Project -> Options for Target -> Debug -> Settings -> Initialization
// 2. 在 "Initialization File" 中指定此文件路径：Scripts/debug_init.ini
//
// 注意：此脚本仅用于调试，实际硬件烧录时不应使用

// 加载程序
LOAD %L INCREMENTAL

// 如果需要，可以在这里设置断点
// 例如：在 main 函数处设置断点
// BS main

// 如果需要跳过 HSE 等待（仅用于仿真），取消下面的注释
// 注意：这会修改硬件寄存器，仅用于仿真环境
// RCC->CR |= RCC_CR_HSERDY;

